# Linear Regression from Scratch (NumPy)

This notebook contains two scratch implementations of Linear Regression using only **NumPy**:

1. **Gradient Descent (iterative optimization)**
2. **Ordinary Least Squares (OLS) / Normal Equation (closed-form solution)**

## Files
- `linear_reg.ipynb` — notebook with both implementations and a small test example

## Requirements
- Python 3.x
- NumPy

Install:
```bash
pip install -r requirements.txt